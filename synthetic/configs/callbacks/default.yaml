defaults:
  - model_checkpoint
  - model_summary
  - lit_progress_bar
  - learning_rate_monitor
  - _self_

model_checkpoint:
  dirpath: ${paths.output_dir}/checkpoints
  monitor: null
  mode: "max"
  save_last: True
  auto_insert_metric_name: True

model_summary:
  max_depth: -1
